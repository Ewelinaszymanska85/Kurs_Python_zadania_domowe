services:
  backend:
    build:
      context: .
      dockerfile: ad_9_Dockerfile
    ports:
      - "8000:8000"
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6380:6379"